{
  "dependencies": {
    "@fortawesome/free-brands-svg-icons": "^5.15.1",
    "@fortawesome/free-solid-svg-icons": "^5.15.1",
    "@fortawesome/vue-fontawesome": "^2.0.0",
    "@nuxtjs/tailwindcss": "^3.4.1",
    "nuxt": "^2.14.12",
    "nuxt-i18n": "^6.16.0"
  },
  "devDependencies": {
    "@nuxtjs/fontawesome": "^1.1.2"
  },
  "name": "hebamme-jana-goellner",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "deploy": "yarn generate && git add --all && git commit -m \"update dist\" && git subtree push --prefix dist origin gh-pages",
    "generate": "nuxt generate",
    "serve": "nuxt",
    "start": "nuxt start"
  },
  "version": "0.1.0"
}
